---
"@firstcrop/preview": minor
---

Added Supabase CMS layer for the storefront: SQL schema with RLS policies, seed script for upserting local JSON into Supabase tables, typed data loaders with JSON fallback, and `.env.example` for local configuration.
