---
"@firstcrop/core": patch
---

Rebranded Tabler to FirstCrop: workspace packages renamed to `@firstcrop/*`, dist filenames renamed (`tabler.css` → `firstcrop.css`, etc.), CSS custom properties renamed (`--tblr-*` → `--fc-*`), and theme localStorage keys renamed (`tabler-*` → `firstcrop-*`).
